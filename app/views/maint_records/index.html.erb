<div class="page-header"><h1>Upload Record</h1></div>

<div class="media">
  <% @maint_records.each do |record| %>
    <div class="media-left">
      <!-- Renders image -->
      <%= link_to image_tag(record.image.url, class: 'media-object'), record.image.url, target: '_blank' %>
    </div>
    <div class="media-body">
      
    </div>
  <% end %>
</div>''
